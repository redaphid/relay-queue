{
  "name": "relay-queue",
  "version": "1.1.0",
  "private": true,
  "description": "Minimal durable local-only HTTP task queue with a mobile web UI, for passing messages between a human and their agents. The core server has zero runtime dependencies; /v2 is a bounded Hono+OpenAPI proof-of-concept, see HONO-POC.md.",
  "license": "MIT",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "up": "docker compose up -d",
    "down": "docker compose down",
    "logs": "docker compose logs -f"
  },
  "dependencies": {
    "@hono/node-server": "^2.1.1",
    "@hono/swagger-ui": "^0.6.1",
    "@hono/zod-openapi": "^1.6.1",
    "hono": "^4.13.3",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "openapi-to-cli": "^0.1.19"
  }
}
